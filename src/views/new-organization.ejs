<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>

        <form action="/new-organization" method="POST">
            <div class="form-group">
                <label for="name">Organization Name</label>
                <!-- Adicionado required e maxlength="150", sem o minlength para permitir teste no servidor -->
                <input type="text" id="name" name="name" required maxlength="150" />
            </div>

            <div class="form-group">
                <label for="description">Description</label>
                <!-- Adicionado required e maxlength="500" -->
                <textarea id="description" name="description" required maxlength="500"></textarea>
            </div>

            <div class="form-group">
                <label for="contactEmail">Contact Email</label>
                <!-- Alterado o tipo para "email" e adicionado required -->
                <input type="email" id="contactEmail" name="contactEmail" required />
            </div>

            <button type="submit">Create Organization</button>
        </form>

    </main>
    <%- include('partials/footer') %>